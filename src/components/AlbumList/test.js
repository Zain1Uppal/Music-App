import { screen } from '@testing-library/react';
import { AlbumList } from '.';

describe('AlbumList', () => {
    beforeEach(() => {
        render(<AlbumList />)
    })

    test('', () => {

    })
})