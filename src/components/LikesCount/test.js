import { screen } from '@testing-library/react';
import { LikesCount } from '.';

describe('LikesCount', () => {
    beforeEach(() => {
        render(<LikesCount />)
    })

    test('', () => {
        
    })
})